IEx.configure(
  history_size: 50,
  inspect: [
    pretty: true,
    limit: 100,
    width: 80
  ],
  alive_prompt: "%prefix(%node)%counter>",
  width: 80,
  default_prompt: "%prefix(%counter)>"
)

# import Ecto.Query
# alias OpenVac.<Any>

